__pycache__/
*.pyc
.venv/
.env
.DS_Store
*.zip

# Regenerated by `python3 run.py` — the built dashboard IS committed (see below)
# so a reviewer can open dashboard/theroux.html straight from a clone, but the
# intermediate JSON it is built from is not worth versioning.
dashboard/results.json
dashboard/runtime.json
dashboard/scenarios/

# Speeches outside the FOMC calendar, parked by the relabelling guard
data/_out_of_coverage/
